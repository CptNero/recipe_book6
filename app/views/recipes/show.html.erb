

  <h1><%= @recipe.title %></h1>


  <pre class="pre-scrollable">
  <code><%=@recipe.description %></code>
  </pre>


<h3>Add a comment:</h3>
<%= render 'comments/form' %>

<h2>Comments</h2>
<%= render @recipe.comments %>



<%if can? :update, @recipe %><%= link_to 'Edit', edit_recipe_path(@recipe) %> |<%end %>
<%= link_to 'Back', recipes_path %>